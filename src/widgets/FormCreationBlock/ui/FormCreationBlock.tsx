'use client'

import React from 'react';
import {FormHeadForm} from "@/features/FormHeadForm";

const FormCreationBlock = () => {
    return (
        <div>
            <FormHeadForm/>
        </div>
    );
};

export default FormCreationBlock;